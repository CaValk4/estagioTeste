let indice = 12;
let soma = 0;
let k =1;

while(k < indice) {
    k = k + 1;
    soma = soma + k;
}

console.log(soma);

//RESPOSTA FINAL: 77

//EXPLICAÇÃO: enquanto k for menor que o indice (12) a variavel k vai somando 1, e adicionando esse valor na soma, ou seja, ele ira somar cada numero até chegar em 12, fazendo um somatório desses numeros.